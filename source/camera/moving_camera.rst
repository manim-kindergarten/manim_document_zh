MovingCamera
============

``MovingCamera`` 是一个可以移动的相机类，显示的范围是一个 ``Rectangle``，
在 :class:`~scene.moving_camera_scene.MovingCameraScene` 中被默认使用。

.. autoclass:: manimlib.camera.moving_camera.MovingCamera
    :members:
